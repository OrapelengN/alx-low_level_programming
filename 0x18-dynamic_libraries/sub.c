#include <stdlib.h>

/**
 * sub - subtracts two integers numbers
 * @a: first int
 * @b: second int
 * Return: the result of the subtraction of two numbers
 */

int sub(int a, int b)
{
	return (a - b);
}
