#include <stdlib.h>

/**
 * mul - multiply two integers together
 * @a: first int
 * @b: second int
 * Return: the result of the multiplication of two numbers
 */

int mul(int a, int b)
{
	return (a * b);
}
